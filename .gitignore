/target
Cargo.lock
crate-hashes.json

# Transient build artifact — regenerated by substrate IFD or `gen lock`.
# Committed only as a deliberate snapshot via `gen lock --snapshot`.
Cargo.build-spec.json
